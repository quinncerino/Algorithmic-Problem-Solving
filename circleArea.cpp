#include<iostream>

using namespace std;

int main()
{
    double radius;
    cout << "Enter the radius: ";
    cin >> radius;
    cout << "The area of the circle is "<< 3.14 * (radius * radius);
    return 0;
}