#include<iostream>

using namespace std;

int main()
{
    int height,length;
    cout << "Enter the height ";
    cin >> height;
    cout << "Enter the length: ";
    cin >> length;
    cout << "the area of the rectangle is " << height * length << endl;
    return 0;
}